# @pseudopilot/api

Modular monolith HTTP/WS API. Must remain horizontally scalable and mostly stateful via Redis.

**Status:** foundation stub. Framework of choice is documented in `docs/architecture/`; not wired yet.
