# API documentation

OpenAPI specs will live here when `apps/api` grows routes.

Milestone 1: placeholder only. Scale rule: every authenticated route carries `RequestContext` (`OrgId` / `UserId`) from `@pseudopilot/shared-types`.
