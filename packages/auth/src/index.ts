/**
 * @pseudopilot/auth — foundation stub (no product behavior yet).
 * Shared auth utilities (JWT/session helpers). Identity providers wired in apps/api later.
 */
export const PACKAGE_NAME = '@pseudopilot/auth' as const;
export const PACKAGE_VERSION = '0.0.0' as const;
