DECLARE D : DATE
D ← SETDATE(4, 10, 2003)
OUTPUT YEAR(D)
OUTPUT MONTH(D)
OUTPUT DAY(D)
IF D = 04/10/2003 THEN
  OUTPUT "date-match"
ENDIF
IF D < SETDATE(5, 10, 2003) THEN
  OUTPUT "date-before"
ENDIF
