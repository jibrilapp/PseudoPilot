/**
 * @pseudopilot/sandbox — foundation stub (no product behavior yet).
 * Adapters for isolated server-side execution (containers / workers).
 */
export const PACKAGE_NAME = '@pseudopilot/sandbox' as const;
export const PACKAGE_VERSION = '0.0.0' as const;
