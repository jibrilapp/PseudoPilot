/**
 * @pseudopilot/interpreter — foundation stub (no product behavior yet).
 * Execution engine (tree-walk / VM) with debugger hooks and instruction budgets.
 */
export const PACKAGE_NAME = '@pseudopilot/interpreter' as const;
export const PACKAGE_VERSION = '0.0.0' as const;
