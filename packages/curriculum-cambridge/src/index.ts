/**
 * @pseudopilot/curriculum-cambridge — foundation stub (no product behavior yet).
 * Versioned Cambridge dialect profiles, keyword tables, and syllabus metadata.
 */
export const PACKAGE_NAME = '@pseudopilot/curriculum-cambridge' as const;
export const PACKAGE_VERSION = '0.0.0' as const;
