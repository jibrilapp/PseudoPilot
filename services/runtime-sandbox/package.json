{
  "name": "@pseudopilot/runtime-sandbox",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Isolated execution microservice. Extracted early as a boundary so sandboxes can scale independently of the API at classroom-peak load.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch --import tsx src/index.ts",
    "test": "vitest run",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist .turbo coverage"
  },
  "dependencies": {
    "@pseudopilot/sandbox": "workspace:*",
    "@pseudopilot/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@pseudopilot/eslint-config": "workspace:*",
    "@pseudopilot/typescript-config": "workspace:*",
    "eslint": "^9.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
